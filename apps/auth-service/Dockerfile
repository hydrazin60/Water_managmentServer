# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @server/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t @server/auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system @server/auth-service && \
          adduser --system -G @server/auth-service @server/auth-service

COPY apps/auth-service/dist @server/auth-service/
RUN chown -R @server/auth-service:@server/auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix @server/auth-service --omit=dev -f install

CMD [ "node", "@server/auth-service" ]
